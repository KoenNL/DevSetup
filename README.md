# Dev Setup

## Installation
- Pull latest changes from master branch
- Run `docker-compose build setup` to build container

## Usage
Run `docker-compose run setup` to create a project. Follow the prompts from there. You can find your project in `/Helper/Output/<project-name>`.
